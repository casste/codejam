#include <iostream>
using namespace std;

int main()
{
    int T;
    cin>>T;
    // cout<<T<<endl;
    for(int t=1;t<=T;t++) {
        // cout<<t<<endl;
        
        int N;
        cin>>N;
        // cout<<N<<endl;

        int n[N],r1=0,r2=0;
        for(int i=0;i<N;i++){
            cin>>n[i];
        }

        for(int i=0;i<N;i++){
            if(n[i-1]>n[i]){
                r1+=n;
                r2+=n;
            }else{
                r2
            }
        }
        cout<<"Case #1: "<<a<<' '<<c<<endl;
    }
    
    return 0;
}
